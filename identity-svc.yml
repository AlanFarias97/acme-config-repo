server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://identity-db:5432/identity_local
    username: identity
    password: identity
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
