server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://identity-db:5432/identity_local
    username: identity
    password: identity
  jpa:
    hibernate:
      ddl-auto: update
  security:
    jwt:
      secret: ${SECURITY_JWT_SECRET:dev-secret-local}
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
