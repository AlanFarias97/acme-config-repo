spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: true
              lower-case-service-id: true
server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway   # habilita /actuator/gateway/**

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
